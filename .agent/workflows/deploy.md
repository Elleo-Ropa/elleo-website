---
description: Deploy changes to GitHub automatically
---

// turbo-all

1. Add all changes
git add .

2. Commit changes
git commit -m "${commit_message}"

3. Push to main branch
git push origin main
